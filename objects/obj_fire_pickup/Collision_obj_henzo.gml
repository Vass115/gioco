/// @description Inserisci qui la descrizione
//Puoi scrivere il tuo codice in questo editor


global.player.slot[1] = "fuoco"

global.player.Damage_Fuoco += 0.1 // se non riesco a fare che la cassa da oggetti diversi aggiungo sto codice per aumentare il danno

// global.max_fire_objects = global.max_fire_objects + 1; //
instance_destroy()